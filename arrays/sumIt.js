// Create a function called sumIt that takes in the array and returns the sum
// of each of the values in the array.

var sumIt = (arr) => arr.reduce((a,b) => a+b);

// DO NOT DELETE
module.exports = sumIt;
