/**
 * Implement a function that runs a "Bubble Sort" on the gicen unsorted array of numbers.
 * https://en.wikipedia.org/wiki/Bubble_sort
 * 
 * Bubble sort works by comparing two adjacent numbers next to each other and then 
 * swapping their places if they are in the wrong order. Keep looping over the array, 
 * as long as values can be swapped.
 * 
 * @param {array} nums - unsorted array
 * @returns {array} sorted nums array
 */

let bubbleSort = (nums) => {

};

// DO NOT DELETE
module.exports = bubbleSort;
